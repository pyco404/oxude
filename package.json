{
  "name": "oxude",
  "version": "0.0.1",
  "private": true,
  "description": "Oxude match engine — agent-versus-agent betting game.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "balance": "tsx scripts/balance.ts",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
